# 1xBet Official Landing Page

Адаптивный одностраничный лендинг для платформы ставок **DRAGONIA**, созданный с нуля без фреймворков.  
Проект демонстрирует современный подход к вёрстке: семантика, доступность, производительность и полная адаптивность.

![Preview](preview.webp)

## 🔧 Технологии

-   **HTML5** — семантическая разметка, ARIA-атрибуты, SEO-дружественная структура
-   **CSS3** — Grid, Flexbox, кастомные градиенты, анимации, медиазапросы
-   **JavaScript** — интерактив без библиотек (слайдер, FAQ, swipe-жесты)
-   **Адаптивность** — поддержка всех устройств (от 320px до 4K)
-   **Оптимизация** — lazy loading, WebP-изображения, минимизация repaints

## 📱 Особенности

-   **Hero-слайдер** с автопрокруткой, точками навигации и поддержкой свайпов на тач-устройствах
-   **Адаптивные блоки фактов**:
    -   На мобильных — сетка (`grid`)
    -   На десктопах — бесконечная горизонтальная карусель с паузой при наведении
-   **Полностью адаптивная таблица** с горизонтальным скроллом (без перестройки на мобильных)
-   **Интерактивный FAQ** с плавным раскрытием
-   **Тёмная тема** с акцентами в фирменных цветах 1xBet (`#22c55e`)
-   **SVG-логотип** в шапке и футере — масштабируется без потерь

## 🗂 Архитектура

-   `index.html` — единый HTML-файл со встроенными стилями и скриптами
-   `scss/blocks/` — компоненты по БЭМ (hero, facts, faq и др.)
-   `images/` — адаптивные изображения и фавиконки
-   `fonts/` — веб-шрифты (при использовании)
-   `site.webmanifest` — манифест для PWA

Проект полностью готов к сборке и масштабированию.

## 🚀 Как запустить

1. Склонируй репозиторий:
    ```bash
    git clone https://github.com/AlexeyPraslov/1xBet-tz
    ```

## 📁 Структура файлов

```
├── scss/               # Исходные SCSS файлы
│   ├── style.scss      # Главный файл
│   ├── global/         # Глобальные стили
│   └── blocks/         # Компоненты
├── js/                 # JavaScript модули
│   ├── main.js         # Точка входа
│   ├── hero-slider.js  # Слайдер
│   └── faq.js          # FAQ аккордеон
├── css/                # Скомпилированные стили
├── images/             # Изображения и иконки
└── fonts/              # Веб-шрифты
```

## 🔧 Рабочий процесс

1. **Редактируйте** SCSS файлы в папке `scss/`
2. **Компилируйте** стили командой `npm run build:css`
3. **Минифицируйте** JS командой `npm run build:js`
4. **Тестируйте** изменения в браузере

Все файлы автоматически оптимизируются для продакшена!
